import { Photo } from "@/components/photo/photo";
import { family } from "@/data/data";

const Family = () => {
  return (
    <>
      <Photo images={family} />
    </>
  );
};
export default Family;
